/* TODO: Not working as expected
pluginRepositories {
	//mavenLocal() is not supported
	maven {
		url '~/.m2/repository'
	}

    gradlePluginPortal()

	maven {
        url 'http://centauri.di.uoa.gr:8081/artifactory/plast'
        credentials {
            username = artifactory_user
            password = artifactory_password
        }
    }
}
*/
rootProject.name = 'source-code-viewer-demo'

